Build a Hacker News dashboard — live front-page stories.

Show about the top 20 items in small cards (title, score, and domain). Auto-refresh every 20 minutes. All cards should be clickable to the original link (`List` + `hrefField`). The link meta should only show the domain.
