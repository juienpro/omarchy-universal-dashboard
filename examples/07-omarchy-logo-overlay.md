Add a shared branding overlay on all views: semi-transparent Omarchy mark, bottom-right.

Float mode (doesn’t push content), opacity around 0.3–0.4. Prefer an `Image` if you have a public logo URL; otherwise use Text “Omarchy” (or an Icon) as the watermark. No view filter; it should survive carousel / view switching.
